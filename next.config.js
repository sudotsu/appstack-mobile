/** @type {import('next').NextConfig} */
const nextConfig = {
  // For Vercel deployment (PWA)
  // output: 'standalone',
  
  // For Capacitor (native Android)
  // Uncomment these lines when building for Android:
  // output: 'export',
  // images: {
  //   unoptimized: true
  // }
}

module.exports = nextConfig
